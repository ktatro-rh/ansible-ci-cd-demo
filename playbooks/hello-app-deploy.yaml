---
- hosts: localhost
  collections:
  - kubernetes.core
  vars:
    image_tag: "{{ image_tag }}"
    
  tasks:
  - name: create deployment
    kubernetes.core.k8s:
      src: ../openshift-resources/app-hello-deployment.yaml
      state: present
